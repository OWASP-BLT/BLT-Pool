{
  "name": "blt-pool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "bash scripts/check_no_runtime_ddl.sh && bash scripts/run-migrations.sh && wrangler deploy",
    "deploy:local": "bash scripts/check_no_runtime_ddl.sh && wrangler d1 migrations apply LEADERBOARD_DB --local && wrangler dev",
    "migrations:apply": "bash scripts/run-migrations.sh",
    "migrations:list": "wrangler d1 migrations list LEADERBOARD_DB --remote",
    "ddl:check": "bash scripts/check_no_runtime_ddl.sh"
  }
}
